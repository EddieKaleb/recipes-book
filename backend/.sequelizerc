const path = require("path");

module.exports = {
  "database": path.resolve("config", "database.js"),
  "models-path": path.resolve("models"),
  "seeders-path": path.resolve("./database/seeders-path"),
};
